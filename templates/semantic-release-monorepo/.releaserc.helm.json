{
  "tagFormat": "helm-v${version}",
  "plugins": [
    [
      "@semantic-release/commit-analyzer",
      {
        "preset": "angular",
        "parserOpts": {
          "noteKeywords": [
            "BREAKING CHANGE",
            "BREAKING CHANGES",
            "BREAKING"
          ]
        }
      }
    ],
    [
      "@semantic-release/release-notes-generator",
      {
        "preset": "angular",
        "parserOpts": {
          "noteKeywords": [
            "BREAKING CHANGE",
            "BREAKING CHANGES",
            "BREAKING"
          ]
        },
        "writerOpts": {
          "commitsSort": [
            "subject",
            "scope"
          ]
        }
      }
    ],
    [
      "@semantic-release/exec",
      {
        "prepareCmd": "./scripts/helm-prepare.sh ${nextRelease.version}",
        "publishCmd": "./scripts/helm-publish.sh ${nextRelease.version}",
        "successCmd": "echo ${nextRelease.version} > ./output/.version"
      }
    ],
    [
      "@semantic-release/changelog",
      {
        "changelogFile": "artifacts/helm/CHANGELOG.md"
      }
    ],
    [
      "@semantic-release/github",
      {
        "successComment": false,
        "failComment": false,
        "assets": [
          {
            "path": "artifacts/helm/*.*"
          }
        ]
      }
    ]
  ]
}
